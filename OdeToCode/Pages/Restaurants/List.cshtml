@page
@model OdeToCode.Pages.Code_sites.ListModel
@{
}

<h1>Restaurants</h1>

<table class="table">
    @foreach(var restaurants in Model.Restaurants)
            {
                <tr>
                    <td>
                        @restaurants.Name
                    </td>
                    <td>
                        @restaurants.Location
                    </td>
                    <td>
                        @restaurants.Cuisine
                    </td>
                </tr>
            }
</table>

<div>@Model.Message</div>